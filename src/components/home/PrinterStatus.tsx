import { PrinterStatusDashboard } from "@/components/equipment/PrinterStatusDashboard";

// Re-export the refactored component for backward compatibility
export function PrinterStatus() {
  return <PrinterStatusDashboard />;
}
